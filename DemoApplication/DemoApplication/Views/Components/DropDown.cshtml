@model DemoApplication.Models.Components.SampleDropdownModel

@{
    ViewBag.Title = "DropDown";
}

<h2>DropDown</h2>

@using (Html.BeginForm("DropDown", "Components", FormMethod.Post, new {@class = "form-horizontal"}))
{
    @Html.EditorForModel()

    <div class="form-actions">
        <button type="submit" class="btn btn-inverse btn-large">Submit</button>
    </div>
}
