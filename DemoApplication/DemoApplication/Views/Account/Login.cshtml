@model DemoApplication.Models.Account.LoginModel
@{
    Layout = "~/Views/Shared/Cassette.cshtml";
    ViewBag.Title = "Login";
}

<div id="login">
    <h3>Login</h3>
    @using (Html.BeginForm())
    {
        @Html.ValidationSummary(true, "", new { @class = "alert alert-block alert-error" })
           
        @Html.EditorForModel()           
                                             
        <a href="@Url.Action("PasswordReset")">Forgot Password ?</a>

        <div class="pull-right">
            <button type="submit" class="btn btn-inverse btn-large">
                Login
				
            </button>
        </div>
            
    }
    ScaffR &copy; 2012
			<div class="pull-right">
                <a href="@Url.Action("Register")">Register</a>
            </div>
</div>
